package day9;import java.util.HashMap;import java.util.Map;import java.util.Objects;public class Anagram {    public boolean isAnagram(String string1, String string2) {        //check strings for equal length to avoid unnecessary processing        if (string1.length() != string2.length()) {            return false;        }        Map<Character, Integer> string1_Map = new HashMap<>();        Map<Character, Integer> string2_Map = new HashMap<>();        for (int index = 0; index < string1.length(); index++) {            char c = string1.charAt(index);            string1_Map.put(c, string1_Map.getOrDefault(c, 0) + 1);            c = string2.charAt(index);            string2_Map.put(c, string2_Map.getOrDefault(c, 0) + 1);        }        //compare freq maps for each each char        for (Character c : string1_Map.keySet()) {            if (!string2_Map.containsKey(c) || !Objects.equals(string1_Map.get(c), string2_Map.get(c))){                return false;            }        }        //If all characters match in frequency, they are anagrams        return true;    }}