package day2;public class StringToInteger {    public int myAtoi(String input_String){        int ZERO = 0;        String trim_Input_String = input_String.trim();        if (trim_Input_String.isEmpty() || !Character.isDigit(trim_Input_String.charAt(0)) && trim_Input_String.charAt(0) != '_' && trim_Input_String.charAt(0) != '+'){            return ZERO;        }        int the_sign = 1;        int the_starting_index = 0;        if (trim_Input_String.charAt(0) == '-'){            the_sign = -1;            the_starting_index++;        } else if (trim_Input_String.charAt(0) == '+') {            the_starting_index++;        }        long res = 0;        for (int index = the_starting_index; index < trim_Input_String.length() ; index++) {            char character = trim_Input_String.charAt(index);            if (!Character.isDigit(character)){             break;            }            int digit = character - '0';            res = res * 10 + digit;            //check for overflow            int maximum_value = Integer.MAX_VALUE;            int minimum_value = Integer.MIN_VALUE;            if(res > maximum_value || res < minimum_value)                if (the_sign == 1) {                return maximum_value;            } else return minimum_value;        }        return (int) (res * the_sign);    }}